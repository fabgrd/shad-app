// Unified onboarding StepOne (cross-platform). Kept as a thin wrapper so the
// existing navigator routes (StepOneIOS / StepOneAndroid) continue to work.
import StepOne from './StepOne'

export default StepOne
