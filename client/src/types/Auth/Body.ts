export type LoginBody = {
    email: string;
    password: string;
};

export type RegisterBody = {
    username: string;
    pseudo: string;
    email: string;
    password: string;
    genre: string;
    birthDate: string;
};